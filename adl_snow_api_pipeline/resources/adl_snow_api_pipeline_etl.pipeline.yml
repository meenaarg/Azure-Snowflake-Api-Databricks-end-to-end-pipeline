# The main pipeline for adl_snow_api_pipeline

resources:
  pipelines:
    adl_snow_api_pipeline_etl:
      name: adl_snow_api_pipeline_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/adl_snow_api_pipeline_etl"

      libraries:
        - glob:
            include: ../src/adl_snow_api_pipeline_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
